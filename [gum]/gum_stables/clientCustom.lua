-- This setting is for when you need a custom function to check 'whether it can be bought.' The condition must return true/false.
function checkProtectHorse()
    -- return true/false
end

function checkProtectCart()
    -- return true/false
end

function protectColoringByVip()
    local isVipEnabled = false
    if isVipEnabled then
        -- Check if the player is in a specific group
        -- If not in an allowed group, return false
        -- return true/false
    else
        return false
    end
end